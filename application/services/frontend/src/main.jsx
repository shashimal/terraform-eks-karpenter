import React from 'react';
import ReactDOM from 'react-dom/client';
import StudentInfoApp from './StudentInfoApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StudentInfoApp />
  </React.StrictMode>
);