# This SecretProviderClass has been moved to the parent sms-app chart
# for centralized secrets management in production.
# See: sms-app/templates/secret-prod.yaml
#
# This consolidation provides:
# - Single SecretProviderClass for all production secrets
# - Reduced Kubernetes resource overhead
# - Centralized secrets configuration
# - Easier maintenance and troubleshooting

{{- if false }}
# Disabled - secrets now managed centrally in parent chart
{{- end }}